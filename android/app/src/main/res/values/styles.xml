<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <!-- Tema base de la aplicación -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Personaliza aquí si lo deseas -->
    </style>

    <!-- Tema sin ActionBar para después del splash -->
    <style name="AppTheme.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <!-- Tema para el splash screen -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="android:windowSplashScreenBackground">#746AB0</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>

   
</resources>
